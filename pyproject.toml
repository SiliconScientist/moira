[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "oasis-mlip"
version = "0.0.1"
authors = [{ name = "Avery Hill", email = "ahill15@tulane.edu" }]
description = "MLIP ingestion and prediction workflows extracted from Oasis."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ase",
    "catbench",
    "matplotlib",
    "numpy",
    "openpyxl",
    "optuna",
    "pandas",
    "polars",
    "pydantic",
    "PyYAML",
    "rootstock",
    "scikit-learn",
    "sympy",
    "torch",
    "tomli; python_version < '3.11'",
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
license = "MIT"
license-files = ["LICEN[CS]E*"]

[project.optional-dependencies]
dev = [
    "pytest>=8",
]

[project.urls]
Homepage = "https://github.com/SiliconScientist/oasis"
Issues = "https://github.com/SiliconScientist/oasis/issues"

[project.scripts]
oasis-mlip = "oasis.mlip.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
